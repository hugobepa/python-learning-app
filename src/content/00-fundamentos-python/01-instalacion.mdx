---
title: "Instalación y Entorno"
description: "Configura Python 3.12+ correctamente"
module: "fundamentos-python"
level: "beginner"
duration: "20 min"
xp: 50
order: 1
---

## 🎯 Objetivos de Aprendizaje

Al finalizar esta lección podrás:

```markdown
1. ✅ Instalar Python 3.12+ y verificar la instalación
2. ✅ Crear entornos virtuales para cada proyecto
3. ✅ Configurar tu editor de código (VS Code)
```

---

## 📚 Teoría Esencial

### ¿Por qué Entornos Virtuales?

| Sin Entorno Virtual       | Con Entorno Virtual        |
| ------------------------- | -------------------------- |
| **Dependencias globales** | Dependencias aisladas      |
| **Conflictos de versión** | Versiones por proyecto     |
| **Difícil de reproducir** | Fácil con requirements.txt |

---

### Estructura de Proyecto Recomendada

```text
mi_proyecto/
├── .venv/              # 🚫 NO subir a Git
├── src/
│   └── main.py
├── tests/
│   └── test_main.py
├── requirements.txt    # ✅ SÍ subir a Git
├── .gitignore
└── README.md
```

---

## 💻 Ejemplo Guiado

### Ejemplo Junior

```bash
# Crear entorno virtual
python -m venv .venv

# Activar (Windows PowerShell)
.venv\Scripts\Activate.ps1

# Activar (macOS/Linux)
source .venv/bin/activate

# Instalar paquete
pip install fastapi
```

### Ejemplo Senior

```bash
# Crear con versión específica
python3.12 -m venv .venv

# Activar y verificar
source .venv/bin/activate
which python

# Instalar con requirements
pip install -r requirements.txt
pip freeze > requirements.txt
```

---

## 🎮 Ejercicio Práctico

**Reto:** Configura tu primer proyecto Python

```markdown
1. Crea una carpeta `python-curso`
2. Dentro, crea un entorno virtual `.venv`
3. Actívalo e instala `fastapi`
4. Crea `src/main.py` con un print
5. Ejecútalo
```

---

## ✅ Criterios de Éxito

```markdown
- [ ] El entorno virtual está activado
- [ ] FastAPI está instalado
- [ ] El script se ejecuta sin errores
```

---

## ✅ Check de Comprensión

**1. ¿Qué comando crea un entorno virtual?**

```markdown
- `python -m venv .venv` ✅
- `pip install venv`
```

**2. ¿Por qué NO se sube `.venv` a Git?**

- Es específico de cada sistema y se puede recrear ✅

---

## 🔗 Para Profundizar

| Recurso                | URL                                                                                |
| ---------------------- | ---------------------------------------------------------------------------------- |
| **Python Official**    | [docs.python.org/3](https://docs.python.org/3/)                                    |
| **venv Documentation** | [docs.python.org/3/library/venv.html](https://docs.python.org/3/library/venv.html) |
| **pip User Guide**     | [pip.pypa.io](https://pip.pypa.io/en/stable/user_guide/)                           |
