---
title: "Autenticación con JWT"
description: "JSON Web Tokens para seguridad de API"
module: "apis-fastapi"
level: "intermediate"
duration: "30 min"
xp: 100
order: 5
---

## 🎯 Objetivos de Aprendizaje

Al finalizar esta lección podrás:

```markdown
1. ✅ Implementar autenticación con JWT
2. ✅ Proteger endpoints con Depends
3. ✅ Hash de contraseñas con bcrypt
```

---

## 📚 Teoría Esencial

### Componentes de Autenticación

| **Componente**    | **Propósito**             | **Librería**       |
| ----------------- | ------------------------- | ------------------ |
| **JWT Token**     | Token de acceso seguro    | `PyJWT`            |
| **Password Hash** | Hashear contraseñas       | `passlib`          |
| **OAuth2**        | Estándar de autorización  | `fastapi.security` |
| **Dependencies**  | Inyección de dependencias | `Depends`          |

---

### Flujo de Autenticación

| **Paso** | **Acción**                    | **Resultado**                   |
| -------- | ----------------------------- | ------------------------------- |
| **1**    | Usuario envía credenciales    | `username`, `password`          |
| **2**    | Servidor valida credenciales  | Verifica en DB                  |
| **3**    | Servidor genera JWT token     | `access_token`                  |
| **4**    | Cliente usa token en requests | `Authorization: Bearer <token>` |

---

## 💻 Ejemplo Guiado

### Instalación de Dependencias

```bash
pip install python-jose[cryptography] passlib[bcrypt] python-multipart
```

### Autenticación Completa

```python
from fastapi import FastAPI, Depends, HTTPException, status
from fastapi.security import OAuth2PasswordBearer, OAuth2PasswordRequestForm
from jose import JWTError, jwt
from passlib.context import CryptContext
from datetime import datetime, timedelta
from pydantic import BaseModel
from typing import Optional

app = FastAPI()

# Configuración
SECRET_KEY = "tu_clave_secreta_muy_segura"
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 30

# Password hashing
pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="token")

class Token(BaseModel):
    access_token: str
    token_type: str

class Usuario(BaseModel):
    username: str
    email: Optional[str] = None
    disabled: Optional[bool] = None

def verificar_password(password, hashed_password):
    return pwd_context.verify(password, hashed_password)

def hash_password(password):
    return pwd_context.hash(password)

def crear_token(data: dict, expires_delta: Optional[timedelta] = None):
    to_encode = data.copy()
    expire = datetime.utcnow() + (expires_delta or timedelta(minutes=15))
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)

@app.post("/token", response_model=Token)
def login(form_data: OAuth2PasswordRequestForm = Depends()):
    # Aquí validarías contra tu base de datos
    usuario_fake = {"username": "admin", "password": hash_password("secret")}

    if form_data.username != "admin" or not verificar_password(form_data.password, usuario_fake["password"]):
        raise HTTPException(status_code=401, detail="Credenciales incorrectas")

    token = crear_token({"sub": form_data.username})
    return {"access_token": token, "token_type": "bearer"}

@app.get("/usuarios/me", response_model=Usuario)
def leer_usuario(token: str = Depends(oauth2_scheme)):
    try:
        payload = jwt.decode(token, SECRET_KEY, algorithms=[ALGORITHM])
        username: str = payload.get("sub")
        if username is None:
            raise HTTPException(status_code=401, detail="Token inválido")
    except JWTError:
        raise HTTPException(status_code=401, detail="Token inválido")

    return {"username": username, "email": "admin@ejemplo.com"}
```

---

## 🎮 Ejercicio Práctico

**Reto:** API protegida con login

Crea una API con:

```markdown
1. Endpoint `/token` para login
2. Endpoint `/usuarios/me` protegido
3. Endpoint `/items/` que requiere autenticación
4. Usa JWT tokens con expiración
```

--JWT-API.py--:

```python
# Tu código aquí
from fastapi import FastAPI, Depends, HTTPException, status
from fastapi.security import OAuth2PasswordBearer, OAuth2PasswordRequestForm
from jose import jwt
from passlib.context import CryptContext
from datetime import datetime, timedelta
from pydantic import BaseModel

app = FastAPI()

SECRET_KEY = "mi_clave_secreta"
ALGORITHM = "HS256"
ACCESS_TOKEN_EXPIRE_MINUTES = 30

pwd_context = CryptContext(schemes=["bcrypt"], deprecated="auto")
oauth2_scheme = OAuth2PasswordBearer(tokenUrl="token")

class Token(BaseModel):
    access_token: str
    token_type: str

def crear_token(data: dict):
    to_encode = data.copy()
    expire = datetime.utcnow() + timedelta(minutes=ACCESS_TOKEN_EXPIRE_MINUTES)
    to_encode.update({"exp": expire})
    return jwt.encode(to_encode, SECRET_KEY, algorithm=ALGORITHM)

@app.post("/token", response_model=Token)
def login(form_data: OAuth2PasswordRequestForm = Depends()):
    if form_data.username != "admin" or form_data.password != "secret":
        raise HTTPException(status_code=401, detail="Credenciales incorrectas")
    token = crear_token({"sub": form_data.username})
    return {"access_token": token, "token_type": "bearer"}

@app.get("/protected")
def protected_route(token: str = Depends(oauth2_scheme)):
    return {"message": "Acceso concedido", "token": token}
```

---

## ✅ Criterios de Éxito

```markdown
- [ ] El login genera tokens JWT
- [ ] Los endpoints protegidos validan el token
- [ ] Los tokens tienen expiración
- [ ] Las contraseñas están hasheadas
```

---

## ✅ Check de Comprensión

**1. ¿Qué librería se usa para JWT en FastAPI?**

```markdown
- `jsonwebtoken`
- `python-jose` ✅
- `jwt-python`
```

**2. ¿Qué esquema OAuth2 usa FastAPI?**

```markdown
- `OAuth2Bearer`
- `OAuth2PasswordBearer` ✅
- `OAuth2Token`
```

**3. ¿Dónde se envía el token en los requests?**

```markdown
- Body del request
- Header `Authorization: Bearer <token>` ✅
- Query parameter
```

---

## 🔗 Para Profundizar

| **Recurso**          | **URL**                                                                                   |
| -------------------- | ----------------------------------------------------------------------------------------- |
| **FastAPI Security** | [fastapi.tiangolo.com/tutorial/security](https://fastapi.tiangolo.com/tutorial/security/) |
| **JWT.io**           | [jwt.io](https://jwt.io/)                                                                 |
| **Passlib Docs**     | [passlib.readthedocs.io](https://passlib.readthedocs.io/)                                 |
